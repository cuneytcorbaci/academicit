version: '3.8'

services:
  # Apache ve PHP servisi
  web:
    image: php:8-apache
    container_name: php-apache-container
    ports:
      - "8080:80"
    volumes:
      - ./web:/var/www/html

  # MySQL servisi
  mysql:
    image: mysql:8.0
    networks:
      - my-network
    container_name: mysql-container
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootroot12345
      MYSQL_DATABASE: database1
      MYSQL_USER: root
      MYSQL_PASSWORD: root12345
    volumes:
      - ./mysql-data:/var/lib/mysql

  # phpMyAdmin servisi
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    networks:
      - my-network
    container_name: phpmyadmin-container
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: P@ssw0rdc
    depends_on:
      - mysql

# Define networks
networks:
  my-network: